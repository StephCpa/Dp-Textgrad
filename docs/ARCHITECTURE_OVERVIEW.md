# DP-TextGrad Architecture Overview

## 🏗️ 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         DP-TextGrad 系统架构                              │
└─────────────────────────────────────────────────────────────────────────┘

                              用户层
┌───────────────────────────────────────────────────────────────────────────┐
│  • get_dp_config("medium_privacy")  # 配置选择                             │
│  • DPEvolutionStrategy(...)         # 优化器创建                           │
│  • optimizer.step()                 # 执行优化                             │
└────────────────────────────────┬──────────────────────────────────────────┘
                                 │
                                 ▼
                          核心组件层
┌───────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐          │
│  │ DPEvolutionStrategy │  │ PrivacyAccountant│  │ ConfigSystem   │         │
│  │  - 主控制器      │  │  - 预算追踪      │  │  - 配置管理    │         │
│  │  - 迭代管理      │  │  - 组合定理      │  │  - 预设参数    │         │
│  └────────┬────────┘  └────────┬────────┘  └─────────────────┘          │
│           │                     │                                          │
│           ▼                     ▼                                          │
│  ┌─────────────────────────────────────────┐                              │
│  │        核心执行流程                       │                              │
│  │  Mutation → Scoring → Selection → Update │                              │
│  └─────────────────────────────────────────┘                              │
└───────────────────────────────────────────────────────────────────────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        ▼                        ▼                        ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ MutationEngine   │  │ DPScorer         │  │ DPSelector       │
│  • 变异生成      │  │  • 评分          │  │  • 选择          │
│  • 不消耗ε       │  │  • 裁剪          │  │  • 消耗ε         │
│                  │  │  • 加噪          │  │                  │
│                  │  │  • 消耗ε         │  │                  │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                     │
         ▼                     ▼                     ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ 变异策略层        │  │ DP机制层          │  │ 选择机制层        │
│  • Adaptive      │  │  • Clipping      │  │  • Noisy Top-k   │
│  • LLM-guided    │  │  • Gaussian      │  │  • Exponential   │
│  • Gradient      │  │  • Sensitivity   │  │  • Gumbel Noise  │
│  • Crossover     │  │                  │  │                  │
└──────────────────┘  └──────────────────┘  └──────────────────┘
```

---

## 🔄 完整优化流程

```
初始化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
┌─────────────────────────────────────────────────────────────────┐
│ 1. 配置加载                                                      │
│    config = get_dp_config("medium_privacy")                      │
│    • ε_target = 2.5                                              │
│    • δ_target = 1e-4                                             │
│    • population_size = 6                                         │
│    • max_iterations = 5                                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. 种群初始化                                                    │
│    Population = [                                                │
│      Variable("Answer the question.", ...),                      │
│      Variable("Respond concisely.", ...),                        │
│      ... (6 candidates)                                          │
│    ]                                                             │
└─────────────────────────────────────────────────────────────────┘

迭代循环（直到预算耗尽或达到最大迭代次数）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────┐
│ 步骤1: 变异生成（不消耗隐私预算）                                │
│                                                                  │
│ Input: 当前种群（6个候选）                                       │
│        ↓                                                         │
│ ┌───────────────────────────────────────────────────┐           │
│ │ MutationEngine.mutate()                           │           │
│ │  • 选择变异策略（Adaptive/LLM/Gradient）         │           │
│ │  • 为每个父代生成2个后代                         │           │
│ │  • 使用LLM理解语义并改进                         │           │
│ └───────────────────────────────────────────────────┘           │
│        ↓                                                         │
│ Output: 12个后代候选                                             │
│                                                                  │
│ 示例：                                                           │
│   Parent: "Answer the question."                                 │
│   ├─ Child1: "Provide a detailed answer with examples"          │
│   └─ Child2: "Think step-by-step before answering"              │
│                                                                  │
│ ε消耗: 0 （不使用私有数据）                                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤2: DP评分（消耗隐私预算）                                    │
│                                                                  │
│ Input: 12个候选                                                  │
│        ↓                                                         │
│ ┌───────────────────────────────────────────────────┐           │
│ │ 2.1 真实评分（使用私有数据）                      │           │
│ │     scores = [evaluate(c, private_data) for c]    │           │
│ │     → [0.45, 0.62, 0.78, 0.51, 0.69, ...]        │           │
│ └───────────────────────────────────────────────────┘           │
│        ↓                                                         │
│ ┌───────────────────────────────────────────────────┐           │
│ │ 2.2 裁剪（控制灵敏度）                            │           │
│ │     clipped = [min(s, C) for s in scores]         │           │
│ │     C = 1.0 (clipping_value)                      │           │
│ │     → [0.45, 0.62, 0.78, 0.51, 0.69, ...]        │           │
│ │     灵敏度: Δf = C = 1.0                          │           │
│ └───────────────────────────────────────────────────┘           │
│        ↓                                                         │
│ ┌───────────────────────────────────────────────────┐           │
│ │ 2.3 加噪（隐私保护）                              │           │
│ │     σ = (Δf/ε)×√(2ln(1.25/δ))                    │           │
│ │     σ = (1.0/0.5)×√(2ln(1.25/1e-5)) ≈ 7.32      │           │
│ │     noisy = clipped + N(0, σ²)                    │           │
│ │     → [0.52, 0.71, 0.65, 0.48, 0.81, ...]        │           │
│ └───────────────────────────────────────────────────┘           │
│        ↓                                                         │
│ Output: DP得分（隐私保护）                                       │
│                                                                  │
│ ε消耗: 0.5                                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤3: DP选择（消耗隐私预算）                                    │
│                                                                  │
│ Input: 12个候选及其DP得分                                        │
│        ↓                                                         │
│ ┌───────────────────────────────────────────────────┐           │
│ │ 方法1: Noisy Top-k                                │           │
│ │   基于DP得分直接选择                              │           │
│ │   selected = top_k(noisy_scores, k=3)             │           │
│ │                                                   │           │
│ │ 方法2: Exponential Mechanism                      │           │
│ │   添加Gumbel噪声后选择                            │           │
│ │   gumbel = rng.gumbel(0, Δ/ε)                    │           │
│ │   selected = top_k(scores + gumbel, k=3)          │           │
│ └───────────────────────────────────────────────────┘           │
│        ↓                                                         │
│ Output: 3个最优候选（作为下一代父代）                            │
│                                                                  │
│ ε消耗: 0.1                                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 步骤4: 更新与记录                                                │
│                                                                  │
│ • 更新种群: Population ← Selected                                │
│ • 记录最优: BestSoFar ← argmax(Selected)                        │
│ • 更新预算: ε_consumed += 0.5 + 0.1 = 0.6                       │
│ • 检查终止条件:                                                  │
│   - ε_consumed ≥ ε_target? → 终止                               │
│   - iteration ≥ max_iterations? → 终止                           │
│   - 否则继续下一轮迭代                                           │
└─────────────────────────────────────────────────────────────────┘

终止与输出
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
┌─────────────────────────────────────────────────────────────────┐
│ 返回最优解                                                       │
│   • optimized_variable: 最优候选                                 │
│   • final_score: 最终得分                                        │
│   • privacy_consumed: (ε=2.3, δ=5e-5)                           │
│   • iterations_completed: 4                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🧬 变异策略详细架构

```
变异引擎（MutationEngine）
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ 策略选择器（create_mutation_function）                  │    │
│  │                                                          │    │
│  │  strategy = "adaptive" | "llm_guided" | "gradient" | ... │    │
│  │                          ↓                               │    │
│  │           返回相应的变异函数                              │    │
│  └────────────────────────────────────────────────────────┘    │
│                          ↓                                       │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ 变异策略实现                                            │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │ AdaptiveMutation   │  │ LLMGuidedMutation  │                │
│  │ ─────────────────  │  │ ──────────────────  │                │
│  │ • 性能追踪         │  │ • 提示构建         │                │
│  │ • 自动模式切换     │  │ • LLM调用          │                │
│  │ • 平台检测         │  │ • 多样性过滤       │                │
│  │                    │  │                    │                │
│  │ 模式切换逻辑:      │  │ 3种模式:           │                │
│  │ ┌──────────────┐  │  │ • EXPLORE (探索)   │                │
│  │ │ 早期: EXPLORE │  │  │ • EXPLOIT (利用)   │                │
│  │ │ 中期: BALANCED│  │  │ • BALANCED (平衡)  │                │
│  │ │ 后期: EXPLOIT │  │  │                    │                │
│  │ │              │  │  │ LLM提示示例:       │                │
│  │ │ 动态调整:     │  │  │ "Generate 2       │                │
│  │ │ • 停滞→EXPLORE│  │  │  improved versions│                │
│  │ │ • 快速→EXPLOIT│  │  │  of: {text}       │                │
│  │ └──────────────┘  │  │  Role: {role}     │                │
│  └────────────────────┘  │  Mode: EXPLORE"   │                │
│                          └────────────────────┘                │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │GradientGuidedMutate│  │CrossoverMutation   │                │
│  │ ────────────────── │  │ ──────────────────  │                │
│  │ • 检查梯度信息     │  │ • 多父代融合       │                │
│  │ • 结合梯度反馈     │  │ • LLM智能组合      │                │
│  │ • 加速收敛         │  │ • 创造新颖解       │                │
│  │                    │  │                    │                │
│  │ 工作流程:          │  │ 示例:              │                │
│  │ if has_gradients:  │  │ Parent1: "Be clear"│                │
│  │   feedback =       │  │ Parent2: "Step-by- │                │
│  │     last_gradient  │  │          step"     │                │
│  │   LLM(parent +     │  │ Parent3: "Examples"│                │
│  │       feedback)    │  │      ↓ LLM融合     │                │
│  │ else:              │  │ Child: "Clear step-│                │
│  │   fallback to      │  │   by-step with     │                │
│  │   LLMGuided        │  │   examples"        │                │
│  └────────────────────┘  └────────────────────┘                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

性能对比
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  简单拼接（基线）          LLM引导                               │
│  ────────────────          ────────                             │
│  性能: ★☆☆☆☆              性能: ★★★★☆                         │
│  收敛: 慢                  收敛: 中                             │
│  提升: 基线                提升: +113%                          │
│                                                                  │
│  梯度引导                  自适应（推荐）                         │
│  ────────                  ──────────────                       │
│  性能: ★★★★☆              性能: ★★★★★                         │
│  收敛: 快                  收敛: 快                             │
│  提升: +93%                提升: +153%                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 差分隐私机制详细架构

```
DP保护层（DPScorer + DPSelector）
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DPScorer: 评分与加噪                                      │  │
│  │ ────────────────────────────────────────────────────────  │  │
│  │                                                            │  │
│  │  Input: 候选变量 + 私有数据                                │  │
│  │     ↓                                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 1. 真实评分                                         │  │  │
│  │  │    raw_scores = [eval(c, private_data) for c]      │  │  │
│  │  │                                                     │  │  │
│  │  │    ⚠️ 此处使用私有数据，需要保护                    │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │     ↓                                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 2. 裁剪（控制灵敏度）                                │  │  │
│  │  │                                                     │  │  │
│  │  │    clipped_scores = []                              │  │  │
│  │  │    for score in raw_scores:                         │  │  │
│  │  │        clipped = min(score, C)                      │  │  │
│  │  │        clipped_scores.append(clipped)               │  │  │
│  │  │                                                     │  │  │
│  │  │    灵敏度: Δf = C (通常 C=1.0)                      │  │  │
│  │  │                                                     │  │  │
│  │  │    原理: 限制单条记录的最大影响                      │  │  │
│  │  │          改变一条记录 → 得分变化 ≤ C                │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │     ↓                                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 3. 加噪（隐私保护）                                  │  │  │
│  │  │                                                     │  │  │
│  │  │    # 计算噪声标准差                                  │  │  │
│  │  │    σ = (Δf / ε) × √(2ln(1.25/δ))                   │  │  │
│  │  │                                                     │  │  │
│  │  │    # 示例计算（ε=0.5, δ=1e-5）                      │  │  │
│  │  │    σ = (1.0/0.5) × √(2×23.95)                      │  │  │
│  │  │      = 2.0 × 6.92                                   │  │  │
│  │  │      ≈ 13.84                                        │  │  │
│  │  │                                                     │  │  │
│  │  │    # 添加高斯噪声                                    │  │  │
│  │  │    noisy_scores = []                                │  │  │
│  │  │    for clipped_score in clipped_scores:             │  │  │
│  │  │        noise = N(0, σ²)                             │  │  │
│  │  │        noisy = clipped_score + noise                │  │  │
│  │  │        noisy_scores.append(noisy)                   │  │  │
│  │  │                                                     │  │  │
│  │  │    原理: 噪声掩盖单条记录的贡献                      │  │  │
│  │  │          相邻数据集 → 输出分布相似                  │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │     ↓                                                      │  │
│  │  Output: DP保护的得分                                     │  │
│  │  Privacy Cost: ε (本轮) + δ (本轮)                        │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                             ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DPSelector: 隐私保护选择                                  │  │
│  │ ────────────────────────────────────────────────────────  │  │
│  │                                                            │  │
│  │  Input: 候选 + DP得分                                      │  │
│  │     ↓                                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 方法1: Noisy Top-k（简单）                          │  │  │
│  │  │                                                     │  │  │
│  │  │    # 直接基于加噪得分选择                            │  │  │
│  │  │    sorted_indices = argsort(noisy_scores)           │  │  │
│  │  │    selected = [candidates[i] for i in               │  │  │
│  │  │                sorted_indices[:k]]                   │  │  │
│  │  │                                                     │  │  │
│  │  │    ε消耗: 0 (噪声已在Scorer中添加)                  │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                    或                                      │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 方法2: Exponential Mechanism（更精确）              │  │  │
│  │  │                                                     │  │  │
│  │  │    # 使用Gumbel噪声                                 │  │  │
│  │  │    sensitivity = max_score - min_score              │  │  │
│  │  │    scale = sensitivity / ε_selection                │  │  │
│  │  │                                                     │  │  │
│  │  │    gumbel_noisy = []                                │  │  │
│  │  │    for score in scores:                             │  │  │
│  │  │        gumbel = rng.gumbel(0, scale)                │  │  │
│  │  │        gumbel_noisy.append(score + gumbel)          │  │  │
│  │  │                                                     │  │  │
│  │  │    selected = top_k(gumbel_noisy, k)                │  │  │
│  │  │                                                     │  │  │
│  │  │    ε消耗: ε_selection                               │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │     ↓                                                      │  │
│  │  Output: 选中的k个候选                                     │  │
│  │  Privacy Cost: ε_selection (通常 0 or 0.1)                │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

隐私预算追踪（PrivacyAccountant）
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 基础组合（Basic Composition）                             │  │
│  │                                                            │  │
│  │  k次查询，每次 (ε₀, δ₀)-DP                                │  │
│  │  总预算: (k·ε₀, k·δ₀)-DP                                  │  │
│  │                                                            │  │
│  │  示例:                                                     │  │
│  │    5次迭代 × (0.5, 1e-5)                                  │  │
│  │    = (2.5, 5e-5)                                          │  │
│  │                                                            │  │
│  │  ❌ 缺点: 过于保守，预算消耗快                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 高级组合（Advanced Composition）✅ 推荐                   │  │
│  │                                                            │  │
│  │  k次查询，每次 (ε₀, δ₀)-DP                                │  │
│  │  总预算: (ε₀√(2k·ln(1/δ')), k·δ₀+δ')-DP                  │  │
│  │                                                            │  │
│  │  示例:                                                     │  │
│  │    5次迭代 × (0.5, 1e-5)                                  │  │
│  │    ε_total = 0.5 × √(2×5×ln(1/1e-5))                     │  │
│  │            = 0.5 × √(10×11.51)                            │  │
│  │            = 0.5 × √115.1                                 │  │
│  │            ≈ 5.37                                          │  │
│  │                                                            │  │
│  │    vs 基础组合: 2.5                                        │  │
│  │    节省: 53%预算！                                         │  │
│  │                                                            │  │
│  │  ✅ 优点: 更紧的界，节省30-50%预算                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  实现:                                                           │
│  class AdvancedCompositionAccountant:                            │
│      def __init__(self, target_epsilon, target_delta):          │
│          self.consumed_epsilon = 0.0                             │
│          self.consumed_delta = 0.0                               │
│          self.query_count = 0                                    │
│                                                                  │
│      def spend(self, epsilon, delta):                            │
│          self.query_count += 1                                   │
│          # 使用高级组合定理计算累计预算                          │
│          self.consumed_epsilon = self._compute_advanced_eps()    │
│          self.consumed_delta += delta                            │
│                                                                  │
│      def has_budget(self):                                       │
│          return (self.consumed_epsilon < self.target_epsilon     │
│                  and self.consumed_delta < self.target_delta)    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 配置系统架构

```
配置管理（config_dp.py）
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DPConfigBundle                                            │  │
│  │ ─────────────────────────────────────────────────────────  │  │
│  │                                                            │  │
│  │  • target_epsilon: float                                   │  │
│  │  • target_delta: float                                     │  │
│  │  • evolution: DPEvolutionConfig                            │  │
│  │  • scorer: DPScorerConfig                                  │  │
│  │  • selector: DPSelectorConfig                              │  │
│  │  • mutation: MutationConfig                                │  │
│  │  • accountant_type: str                                    │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 预设配置                                                   │  │
│  │ ─────────                                                  │  │
│  │                                                            │  │
│  │  ┌────────────────────┐  ┌────────────────────┐          │  │
│  │  │ high_privacy       │  │ medium_privacy     │          │  │
│  │  │ ──────────────────  │  │ ────────────────── │          │  │
│  │  │ • ε = 1.0          │  │ • ε = 2.5          │          │  │
│  │  │ • δ = 1e-5         │  │ • δ = 1e-4         │          │  │
│  │  │ • pop = 4          │  │ • pop = 6          │          │  │
│  │  │ • iter = 3-5       │  │ • iter = 5-8       │          │  │
│  │  │                    │  │                    │          │  │
│  │  │ 适用: 医疗、金融    │  │ 适用: 通用业务     │          │  │
│  │  └────────────────────┘  └────────────────────┘          │  │
│  │                                                            │  │
│  │  ┌────────────────────┐  ┌────────────────────┐          │  │
│  │  │ low_privacy        │  │ minimal_privacy    │          │  │
│  │  │ ──────────────────  │  │ ────────────────── │          │  │
│  │  │ • ε = 5.0          │  │ • ε = 10.0         │          │  │
│  │  │ • δ = 1e-4         │  │ • δ = 1e-4         │          │  │
│  │  │ • pop = 8          │  │ • pop = 10         │          │  │
│  │  │ • iter = 8-12      │  │ • iter = 10+       │          │  │
│  │  │                    │  │                    │          │  │
│  │  │ 适用: 聚合数据      │  │ 适用: 测试、公开   │          │  │
│  │  └────────────────────┘  └────────────────────┘          │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 使用接口                                                   │  │
│  │ ────────                                                   │  │
│  │                                                            │  │
│  │  # 获取预设配置                                             │  │
│  │  config = get_dp_config("medium_privacy")                  │  │
│  │                                                            │  │
│  │  # 自定义覆盖                                               │  │
│  │  config = get_dp_config(                                   │  │
│  │      "medium_privacy",                                     │  │
│  │      custom_overrides={                                    │  │
│  │          "target_epsilon": 3.0,                            │  │
│  │          "population_size": 8                              │  │
│  │      }                                                     │  │
│  │  )                                                         │  │
│  │                                                            │  │
│  │  # 转换为优化器参数                                         │  │
│  │  optimizer_kwargs = config.to_optimizer_kwargs()           │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 验证框架架构

```
隐私验证系统（evaluation/）
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ privacy_verification.py                                   │  │
│  │ ─────────────────────────────────────────────────────────  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ Test 1: NeighboringDatabaseTest                    │  │  │
│  │  │                                                     │  │  │
│  │  │  验证核心DP属性:                                     │  │  │
│  │  │  Pr[M(D)] ≤ e^ε × Pr[M(D')] + δ                    │  │  │
│  │  │                                                     │  │  │
│  │  │  方法:                                               │  │  │
│  │  │  1. 创建数据集D和D'（差一条记录）                    │  │  │
│  │  │  2. 在D和D'上运行优化                               │  │  │
│  │  │  3. 比较输出分布                                     │  │  │
│  │  │  4. 验证是否满足DP定义                               │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ Test 2: MembershipInferenceTest                    │  │  │
│  │  │                                                     │  │  │
│  │  │  模拟攻击:                                           │  │  │
│  │  │  攻击者尝试推断某条记录是否在训练集中                │  │  │
│  │  │                                                     │  │  │
│  │  │  方法:                                               │  │  │
│  │  │  1. 训练两个模型（有/无目标记录）                     │  │  │
│  │  │  2. 观察输出差异                                     │  │  │
│  │  │  3. 攻击者基于差异猜测成员关系                       │  │  │
│  │  │  4. 验证攻击成功率 ≤ 理论上界                       │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ Test 3: NoiseDistributionTest                      │  │  │
│  │  │                                                     │  │  │
│  │  │  验证噪声机制:                                       │  │  │
│  │  │  添加的噪声是否符合理论分布                          │  │  │
│  │  │                                                     │  │  │
│  │  │  方法:                                               │  │  │
│  │  │  1. 多次运行获取噪声样本                             │  │  │
│  │  │  2. 拟合噪声分布                                     │  │  │
│  │  │  3. KS检验：实际 vs 理论高斯分布                    │  │  │
│  │  │  4. 验证 σ 参数正确                                 │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ Test 4: PrivacyBudgetAccuracyTest                  │  │  │
│  │  │                                                     │  │  │
│  │  │  验证预算追踪:                                       │  │  │
│  │  │  Accountant计算是否准确                             │  │  │
│  │  │                                                     │  │  │
│  │  │  方法:                                               │  │  │
│  │  │  1. 运行k次查询                                      │  │  │
│  │  │  2. 记录消耗的ε和δ                                  │  │  │
│  │  │  3. 对比理论值（组合定理）                           │  │  │
│  │  │  4. 验证误差在可接受范围                             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 快速检查工具                                               │  │
│  │ ───────────────                                            │  │
│  │                                                            │  │
│  │  quick_privacy_check.py (~30秒)                            │  │
│  │  • 噪声注入检查                                             │  │
│  │  • 裁剪机制检查                                             │  │
│  │  • 预算追踪检查                                             │  │
│  │  • DP选择检查                                              │  │
│  │  • 预算耗尽检查                                             │  │
│  │                                                            │  │
│  │  返回: PASS/FAIL（适合CI/CD集成）                          │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↓                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 可视化报告                                                 │  │
│  │ ─────────                                                  │  │
│  │                                                            │  │
│  │  privacy_visualization.py                                  │  │
│  │  • 生成4面板摘要图                                          │  │
│  │  • 详细测试结果图                                           │  │
│  │  • 导出JSON报告                                            │  │
│  │  • 导出文本报告                                             │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📚 文档组织结构

```
DP-TextGrad 文档系统
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  入门级 (⭐)                                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • README.md              - 快速开始                        │  │
│  │ • QUICK_REFERENCE.md     - 一页速查                        │  │
│  │ • dp_es_quickstart.py    - 最小示例                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  教程级 (⭐⭐)                                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Tutorial-DP-Evolution-Strategy.ipynb - 基础教程          │  │
│  │ • Tutorial-DP-ES-Comparison.ipynb      - 性能对比          │  │
│  │ • MUTATION_UPGRADE_GUIDE.md            - 升级指南          │  │
│  │ • interactive_dp_explanation.py        - 交互演示          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  进阶级 (⭐⭐⭐)                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • MUTATION_STRATEGIES.md              - 变异策略详解       │  │
│  │ • advanced_mutation_example.py        - 高级示例           │  │
│  │ • README_PRIVACY_VERIFICATION.md      - 验证指南           │  │
│  │ • config_dp.py                        - 配置系统源码       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  专家级 (⭐⭐⭐⭐)                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • DP_ES_TECHNICAL_EXPLANATION.md     - 技术深入            │  │
│  │ • IMPLEMENTATION_SUMMARY.md          - 实现总结            │  │
│  │ • ARCHITECTURE_OVERVIEW.md           - 架构图解（本文档）   │  │
│  │ • mutation_strategies.py             - 变异策略源码        │  │
│  │ • privacy_verification.py            - 验证框架源码        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  学习路径建议:                                                   │
│  新手: 1 → 2 → 3 → 4                                            │
│  进阶: 5 → 6 → 7 → 8                                            │
│  专家: 9 → 10 → 11 → 12                                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 设计原则

### 1. 模块化

```
每个组件独立且可替换:
┌────────────┐  ┌────────────┐  ┌────────────┐
│ Mutation   │  │ Scoring    │  │ Selection  │
│ Engine     │  │ System     │  │ System     │
└────────────┘  └────────────┘  └────────────┘
      ↕              ↕              ↕
  可替换        可配置        可扩展
```

### 2. 隐私优先

```
隐私保护贯穿全流程:
✓ 默认使用高级组合（节省预算）
✓ 自动预算追踪
✓ 超预算立即停止
✓ 验证框架内置
```

### 3. 易用性

```
一行配置，开箱即用:
config = get_dp_config("medium_privacy")
optimizer = DPEvolutionStrategy(**config.to_optimizer_kwargs())
optimizer.step()
```

### 4. 可扩展性

```
支持自定义扩展:
• 自定义变异策略
• 自定义评分函数
• 自定义选择机制
• 自定义验证测试
```

---

## 📊 性能特征

### 时间复杂度

```
单次迭代:
- 变异生成: O(pop_size × offspring × LLM_time)
  典型: 6 × 2 × 1s = 12s

- DP评分: O(pop_size × eval_time)
  典型: 12 × 0.5s = 6s

- DP选择: O(pop_size × log(pop_size))
  典型: 12 × log(12) ≈ 0.001s

总计: ~18-20s/iteration
```

### 空间复杂度

```
内存占用:
- 种群存储: O(pop_size × text_length)
  典型: 10 × 1KB = 10KB

- 历史记录: O(iterations × pop_size)
  典型: 5 × 10 × 1KB = 50KB

总计: < 1MB（非常轻量）
```

### 成本分析

```
LLM API成本（gpt-4o-mini）:
- 变异: $0.15/$0.60 per 1M tokens (in/out)
- 每次变异 ~500 tokens
- 典型优化: 60次变异 × $0.0003 ≈ $0.02

总成本: $0.02-0.05/optimization
```

---

## 🔗 组件交互图

```
用户交互流
════════════════════════════════════════════════════════════════════

用户代码
  │
  ├─ get_dp_config("medium_privacy")
  │    └→ ConfigSystem → DPConfigBundle
  │
  ├─ create_mutation_function("adaptive")
  │    └→ MutationFactory → AdaptiveMutation
  │
  ├─ DPEvolutionStrategy(...)
  │    └→ 初始化所有组件
  │
  └─ optimizer.step()
       │
       └→ 主循环开始
            │
            ├─ MutationEngine.mutate()
            │   └→ LLM调用，生成后代
            │
            ├─ DPScorer.score()
            │   ├→ 真实评分（用户函数）
            │   ├→ 裁剪
            │   └→ 加噪
            │
            ├─ DPSelector.select()
            │   └→ DP选择
            │
            ├─ PrivacyAccountant.spend()
            │   └→ 更新预算
            │
            └─ 检查终止条件
                 ├─ 预算耗尽？
                 ├─ 达到最大迭代？
                 └─ 返回最优解

内部数据流
════════════════════════════════════════════════════════════════════

Variable对象流:
初始化 → 变异 → 评分 → 选择 → 下一代
  ↓       ↓      ↓      ↓       ↓
[text]  [texts] [scores] [selected] [text]

隐私预算流:
初始 ε_target=2.5
  ↓
迭代1: ε_consumed=0.6
  ↓
迭代2: ε_consumed=1.2
  ↓
迭代3: ε_consumed=1.8
  ↓
迭代4: ε_consumed=2.4
  ↓
停止（预算即将耗尽）
```

---

## 📖 总结

本文档提供了 DP-TextGrad 系统的完整架构视图，涵盖：

1. **系统架构总览** - 整体组件和层次结构
2. **优化流程详解** - 从初始化到终止的完整流程
3. **变异策略架构** - 4种智能变异策略的实现细节
4. **差分隐私机制** - DP保护的完整流程和原理
5. **配置系统** - 预设配置和自定义机制
6. **验证框架** - 4类测试和快速检查工具
7. **文档组织** - 分层文档和学习路径
8. **设计原则** - 模块化、隐私优先、易用性、可扩展性
9. **性能特征** - 时间、空间、成本分析
10. **组件交互** - 用户交互和内部数据流

**推荐阅读顺序:**
1. 本文档（架构总览）
2. `QUICK_REFERENCE.md`（快速参考）
3. 相关详细文档（根据需求选择）

---

**最后更新**: 2025-11-03
